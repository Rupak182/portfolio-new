---
title: Load Balancing
date: 2026-07-17
description: Learn about load balancing algorithms, health checks, and high availability.
tags: ["System Design", "Load Balancing", "Nginx"]
---

Load balancers act as the traffic cop in front of your servers, routing client requests across all servers capable of fulfilling those requests in a manner that maximizes speed and capacity utilization.

## Routing Algorithms

*   **Round Robin**: Requests are distributed sequentially.
*   **Least Connections**: Directs traffic to the server with the fewest active connections.
*   **IP Hashing**: Derives a hash from the client's IP address to determine which server gets the request, ensuring session persistence.
*   **Least Response Time**: Directs requests to the server with the fewest active connections and the lowest average response time.

## High Availability Configurations

To prevent the load balancer itself from becoming a single point of failure (SPOF), load balancers are usually deployed in high-availability clusters:
*   **Active-Passive**: One active load balancer handles traffic, while a passive node monitors its health. If the active node fails, the passive node takes over the virtual IP.
*   **Active-Active**: Multiple load balancers process traffic simultaneously, with DNS round-robin distributing incoming requests across them.
